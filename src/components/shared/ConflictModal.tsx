'use client'

// ConflictModal is superseded by the inline ConflictPanel in the 4-phase pipeline.
// This stub remains to avoid breaking imports in files that haven't been updated.
export function ConflictModal() {
  return null
}
